{
  "name": "NGHTTP2",
  "dockerFile": "Dockerfile",

  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },

  "extensions": [
    "ms-python.python"
  ],

  "runArgs": [
    "--privileged"
  ],

  "remoteUser": "vscode"
}
